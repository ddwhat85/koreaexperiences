"""publisher/make_connector.pyTrafficAI Engine 1.0 — Make.com Webhook ConnectorData flow: Python Engine -> Make.com Webhook -> Google Sheets log + Buffer -> Threads"""import osimport reimport jsonimport loggingimport timefrom datetime import datetime, timedelta, timezonefrom dataclasses import dataclass, asdict, fieldfrom typing import Optionalimport requestsfrom requests.adapters import HTTPAdapterfrom urllib3.util.retry import Retrylogger = logging.getLogger(__name__)PUBLISH_SLOTS = ["08:10", "12:05", "15:05", "18:20", "22:10"]@dataclassclass FAQItem:    question: str    answer: str@dataclassclass PublishPacket:    product_id: str    store_name: str    product_name: str    image_url: str
